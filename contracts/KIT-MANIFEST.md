# Kit Manifest

A kit manifest describes one official installable kit.

Required fields: id, domain, kind, stability, factory, entry, provides, requires.

Optional fields: cdn, source, description, version, examples, tests.
