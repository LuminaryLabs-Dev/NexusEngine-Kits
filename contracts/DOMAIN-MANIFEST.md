# Domain Manifest

A domain manifest describes one installable domain group.

Required fields: id, label, kits.

Recommended fields: description, kind, exports, examples, status.

A domain manifest should list related kits and explain how the domain is installed by package and CDN.
