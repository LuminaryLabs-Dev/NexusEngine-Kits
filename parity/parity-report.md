# Parity Report

## Current state

The clean rebuild structure is in place. `generic-resource-loop-kit` is official, `completion-ledger-kit` remains candidate, and default installation now excludes unready entries.

## Current parity level

```txt
catalog parity: partial
installer parity: scaffolded
behavior parity: 1 of 120 baseline kits resolved
CDN parity: scaffolded
per-kit docs parity: partial
```

## Next required work

1. Upgrade the registry, capability-graph, and composition-planning control-plane DSKs in ProtoKits.
2. Promote those additions only after repository-registry and dependency-plan proof.
3. Continue the 119 unresolved baseline kits in dependency waves.
4. Keep `npm run progress` and the machine parity records aligned.
