# Dynamic Install

Dynamic install turns a kit id into an installable kit.

The installer resolves a manifest, finds a factory, creates a runtime kit, installs it into the engine, and returns a report.

During the rebuild, unresolved kits can install as metadata-backed placeholders.
