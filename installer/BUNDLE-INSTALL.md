# Bundle Install

Bundle install creates and installs every domain listed for a bundle.

## API

```js
await installer.installBundle(engine, "default-game-stack");
```

## Report

The bundle report should include one child report per domain.
