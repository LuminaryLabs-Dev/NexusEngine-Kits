# Behavior Audit

Behavior audit checks that documented behavior is implemented and tested.

The first real target is `completion-ledger-kit`.

Run:

```txt
npm run audit:behavior
```

The report is written to `audit/reports/completion-ledger-behavior-report.md`.
