# Known Gaps Audit

Known gaps audit checks that top-level limitation and gap documents exist and that real/candidate status is not overstated.

Run:

```txt
npm run audit:gaps
```

This audit does not require every future kit to be complete. It requires known incompleteness to be visible.
