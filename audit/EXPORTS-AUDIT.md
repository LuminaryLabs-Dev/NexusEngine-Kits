# Exports Audit

Exports audit checks that every public package export points to an existing file.

It also checks required public surfaces such as installer, bundles, domain-spatial, and completion-ledger-kit.

Run:

```txt
npm run audit:exports
```
