name: Reconcile seeded world patch catalog

on:
  push:
    branches: [main]
    paths:
      - .agent/reconcile-seeded-world-patch

permissions:
  contents: write

jobs:
  reconcile:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Generate catalogs and mirrors
        run: node scripts/build-catalog.mjs
      - name: Validate generated kit syntax
        run: |
          node --check kits/simulation/seeded-world-patch-controller-kit/index.js
          node --check kits/simulation/seeded-world-patch-controller-kit/smoke.test.mjs
          node scripts/validate-catalog.mjs
          node scripts/validate-manifests.mjs
          node scripts/check-exports.mjs
      - name: Commit generated files
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add installer/generated-catalog.js installer/generated-factories.js \
            kit-catalog.json domain-catalog.json bundle-catalog.json \
            nexusengine.registry.json promotion-ledger.json parity/parity-status.json \
            PROMOTION-LEDGER.md docs/CDN-INDEX.txt \
            kits/simulation/seeded-world-patch-controller-kit/kit.json
          if git diff --cached --quiet; then
            echo "Generated files already reconciled."
            exit 0
          fi
          git commit -m "Rebuild seeded world patch catalogs"
          git push origin HEAD:main
