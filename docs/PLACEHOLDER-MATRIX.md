# Placeholder Matrix

The root `PLACEHOLDER-MATRIX.md` tracks which catalog entries are real, scaffolded, or placeholder-only.

Use:

```txt
npm run generate:placeholder-matrix
```

to regenerate the matrix after catalog or kit status changes.
