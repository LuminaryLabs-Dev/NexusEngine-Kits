# Intention

Status: active

Maintain NexusEngine-Kits as the evidence-backed stable catalog, not a mirror of every experimental idea. Expand stable capability coverage by promoting complete feature unions into bounded domains with deterministic, inspectable behavior.

Keep registry metadata discovery separate from module execution and NexusEngine runtime installation. Preserve reproducibility through immutable commit resolution, generated integrity, and lockfile replay.
