# Source parity

The provider implements the Nexus multiplayer transport shape with injected PeerJS and portable JSON messages. The fake-Peer smoke proof validates channel options, lifecycle, delivery callbacks, and cleanup without external services.
