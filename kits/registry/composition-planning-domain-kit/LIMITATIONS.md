# Limitations

- Produces plans only; module resolution and `engine.installKit()` remain installer responsibilities.
- Status opt-ins must be explicit. Placeholder and scaffold entries remain non-executable even when selected.
- Alternative provider policy is intentionally outside the canonical deterministic default.
