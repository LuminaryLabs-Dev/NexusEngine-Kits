# Source Parity

Validated source: NexusEngine-ProtoKits commit `9b0d74c57c29d04a9ba955ae41c8ca19cb2cd5d6`.

Preserved behavior:

- numeric normalization, clamp, interpolation, and exponential approach
- list normalization, structured cloning, and 2D distance
- bounded clock accessors
- injected or fallback runtime definitions
- resource initialization
- deterministic hashing, scoped seeds, stable IDs, and ID lookup
- seeded random values and weighted choice
- native installation, replacement metadata, reset, snapshot, and load

Canonical replacements remain in NexusEngine. No useful source behavior was removed; the stable status is deprecated because this aggregate is a migration bridge rather than a domain owner.
