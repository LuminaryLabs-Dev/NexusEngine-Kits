# Limitations

- Inventory settlement is intentionally external and must apply the plan's `resourceChanges`.
- Weather and calendar generation are external; daily growth consumes explicit day and rainfall inputs.
- This version models plot-level soil, not spatial soil simulation inside a plot.
- Animal husbandry is not included.
- Rendering, animation, UI, audio, and persistence transport are not included.
