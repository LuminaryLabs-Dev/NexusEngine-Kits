# Source parity

Promoted from:

```txt
LuminaryLabs-Agents/NexusEngine-ProtoKits
commit b96efb2e3a6059c65dbc3e37d5bf051ce38030a0
protokits/agriculture-domain-kit/index.js
```

The official implementation preserves the ProtoKit API, services, events, snapshot schema, deterministic yield rules, continuous and daily growth modes, annual and perennial behavior, and renderer boundary. The only default change is promotion stability from `protokit` to `official`.
