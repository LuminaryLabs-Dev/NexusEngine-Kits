# Registry Domain

Official metadata control plane for NexusEngine kit discovery and deterministic composition.

Install order is fixed: `kit-registry-domain-kit`, `capability-graph-domain-kit`, then `composition-planning-domain-kit`. Repository transport, integrity verification, lockfiles, module loading, and runtime installation remain in the installer/registry adapter layer.
