# Procedural Objects Domain

Official renderer-agnostic services for portable procedural object bodies, material meaning, LOD policy, and capture profiles. The domain consumes NexusEngine's `nexus-object-descriptor/1` contract and leaves renderer objects, GPU capture, physics resolution, and asset transport to adapters.
